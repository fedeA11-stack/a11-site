# Implementation Plans

Generated by the improve skill on 2026-06-16 (commit `0c23ed1`, branch
`tech-optimization`). Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

These three are the "quick wins" batch — all small, low-risk, high-confidence
tech-optimization cleanups. They are mutually independent (no plan depends on
another), but the recommended order does tooling first so the lint/typecheck
gates the later plans use for verification are reliable.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Scope ESLint to app source + add `typecheck` script | P1 | S | — | DONE (executor commit `dd0e7e3`, branch `worktree-agent-a7773566c9de72dab`, base `0c23ed1`) — reviewed & approved, not yet merged |
| 002  | Delete unused `CraftButton` / `ProjectImage` components | P1 | S | — | DONE (executor commit `993f3fd`, branch `worktree-agent-a72a33ad465429ad7`, base `0c23ed1`) — reviewed & approved, not yet merged |
| 003  | Remove ~71 MB of unreferenced assets from `public/` | P1 | S | — | DONE (executor commit `fa39f9c`, branch `worktree-agent-a77503f896fdf51cd`, base `62b6bd6`=main) — reviewed & approved, not yet merged |
| 004  | Image & font loading optimization (next/image + next/font) | P1 | M | — | DONE (branch `feat/loading-optimization`, base `c6e5cd1`) — eng-reviewed, build/typecheck/lint/preview-verified, not yet committed |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Execution note (2026-06-16)

All three plans executed via isolated-worktree subagents and reviewed (diffs
re-verified against scope). Each executor commit is clean and in scope; none
has been merged — that is the user's decision.

**Base-commit caveat:** `main` advanced to `62b6bd6` (PRs #6 + #7, incl. a new
`/manifesto` route) *during* execution. The `tech-optimization` branch is 4
commits behind main. Plans 001/002 were built on `0c23ed1`; plan 003 on
`62b6bd6`. The 003 deletions are independent of app code (the executor's
space-safe scan re-ran against the newer `app/`, which already included
manifesto, and `npm run build` passed), so they are safe on either base.
Recommended landing: bring `tech-optimization` up to `main` (`62b6bd6`) first,
then apply all three changes onto it.

## Dependency notes

- No hard dependencies between these three. Recommended order is 001 → 002 →
  003 only so that `npm run typecheck` (added in 001) exists as a verification
  command for 002 and 003. If you run 002/003 first, substitute
  `npx tsc --noEmit` for `npm run typecheck`.

## Findings audited but NOT planned (this batch)

The user selected only the quick-wins bundle. The following vetted findings
were deferred — they are real and worth doing, just larger/riskier:

- **next/image migration** (perf, L): all images use raw `<img>` (~30 sites);
  home page alone ships 6 PNGs at 2.4–5.2 MB. The single biggest perf lever.
  Deferred for effort + risk (the `/world` cove `clip-path` interaction with
  `<Image fill>` needs care).
- **next/font migration** (perf, M): 8 OTF `@font-face` declarations in
  `globals.css` should move to `next/font/local` for fallback-metric CLS fix
  and managed preload. README falsely claims `next/font` is already used.
- **Drop needless `"use client"`** (tech-debt, M): `world/money` and `studio`
  have zero interactivity yet are client components; interactivity elsewhere
  could be isolated into small client islands.
- **Deduplicate design tokens** (tech-debt, M): `FONT` const in 6 files,
  `T.h1/h2/body` objects redefined per page (with drift, e.g. stray
  `fontWeight: 450`), `#282328` hardcoded ~40×; unused `--font-system` theme
  var in `globals.css`.
- **Non-functional / inaccessible CTAs** (correctness/a11y, S): home CTA
  `<button>`s have no handler/href; studio "Read our Manifesto" is a `<div>`
  (not keyboard-accessible). Partly product-incomplete (placeholders).

## Findings considered and rejected

- **Security audit**: no findings. Static marketing site — no backend, no
  secrets, no user input; the only external surfaces are a `mailto:` link and
  one hotlinked `flagcdn.com` flag image. Nothing to remediate.
- **Shrinking git history** of the large assets: deliberately out of scope —
  `git filter-repo` is high-risk and not warranted for a portfolio repo.

---

# Batch 2 — Performance, best practices & SEO (2026-06-23, commit `a6d217d`, branch `perf-seo-optimization`)

Generated by the improve skill, scoped to performance + best practices + best
SEO. Plans 005–012. Findings were vetted against source before planning. Six
quick-win plans were executed via isolated-worktree subagents and reviewed
(diffs re-verified against scope); the two heavy media plans (005, 009) are not
yet executed.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 007  | Homepage semantic `<h1>` | P1 | S | LOW | — | DONE (executor commit `48a8bfe`, branch `worktree-agent-ace31183356c2cd9b`, base `a6d217d`) — reviewed & approved, not yet merged |
| 006  | Production `next.config.ts` (AVIF, hardening, asset cache) | P1 | S | LOW | (see 005 note) | DONE (executor commit `14e218c`, branch `worktree-agent-af840d3b10e079d5e`, base `a6d217d`) — reviewed & approved, not yet merged |
| 008  | `next/image` `sizes` account for full-bleed gutter | P2 | S | LOW | — | DONE (executor commit `f2ef353`, branch `worktree-agent-a5f69a38d429bfcd5`, base `a6d217d`) — reviewed & approved, not yet merged |
| 010  | SEO metadata gaps (contact sitemap+OG, Nous OG image) | P2 | S | LOW | — | DONE (executor commit `95ca20d`, branch `worktree-agent-a5d01e05669d97bcb`, base `a6d217d`) — reviewed & approved, not yet merged |
| 011  | Fix structured-data contact email → `hello@a11.studio` | P2 | S | LOW | — | DONE (executor commit `4711a7f`, branch `worktree-agent-a22c2616ed8b0b854`, base `a6d217d`) — reviewed & approved, not yet merged |
| 012  | Add `app/error.tsx` + `app/not-found.tsx` | P2 | S | LOW | — | DONE (executor commit `bebb1fb`, branch `worktree-agent-ada725f8aa950a715`, base `a6d217d`) — reviewed & approved, not yet merged |
| 009  | Compress oversized source images (build/deploy weight) | P3 | S | LOW | — | TODO |
| 005  | Shrink + harden self-hosted videos | P1 | M | MED | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Execution & review note (2026-06-23)

Plans 006, 007, 008, 010, 011, 012 each ran in an isolated worktree (sonnet
executor) and were reviewed as a unit. For each: the committed diff touches only
in-scope files, matches the plan exactly, `next build` exits 0 (which type-checks
the routes), and lint shows **no new problems** vs. the baseline. One documented,
approved deviation: plan 012's `not-found.tsx` escapes apostrophes as `&apos;` to
avoid `react/no-unescaped-entities` — consistent with App Router conventions and
in service of the plan's intent.

**Plan-criterion correction:** every Batch 2 plan listed "`npm run lint` exits 0"
as a done criterion. That was a recon error — lint already fails on `main` with
**4 pre-existing errors** (see deferred finding below). The criterion was waived
in review; the real standard applied was "introduces no new lint/type problems,"
which all six meet.

**Not yet executed:** 005 (videos) needs `ffmpeg` + a video-quality spot-check
(MED risk). 009 (source-image compression) is P3 build/deploy weight only.
**Dependency:** land 005 before 006 if deploying, because 006 adds a 1-year
`immutable` cache on `/assets/*` and 005 re-encodes some videos in place (same
filename) — otherwise clients could pin a stale large video.

## Findings audited but deferred (Batch 2)

- **Preloader blocks first paint ~1.7s + locks scroll** (`app/Preloader.tsx`,
  perf, M): genuine LCP cost but a deliberate brand moment — a product/design
  decision, not a mechanical fix.
- **`unstable_instant` + Cache Components** (perf, L, direction): this Next.js
  fork hints at it (`node_modules/next/dist/docs/index.md:11`); `draft` feature,
  modest payoff for already-static pages.
- **Custom-cursor accessibility** (`app/Cursor.tsx`, a11y, M): `cursor:none`
  site-wide, no `:focus-visible` keyboard fallback.
- **WebSite/Breadcrumb JSON-LD on top-level pages** (SEO, S): sitelinks
  searchbox needs a real search endpoint (none exists); low yield.
- **Structured logging in the contact action** (`actions.ts`, obs, M):
  `console.error` only; observability backlog.
- **NEW — 4 pre-existing react-hooks lint errors** (correctness, S): discovered
  during Batch 2 execution. `react-hooks/set-state-in-effect` in
  `app/contact/CopyEmail.tsx:24`, `app/CaseStudy.tsx:194`, `app/NavMenu.tsx:232`,
  and `react-hooks/refs` "Cannot access refs during render" in `app/NavMenu.tsx:432`.
  These predate Batch 2 and make `npm run lint` exit non-zero on `main`. Worth a
  dedicated cleanup plan.

## Findings considered and rejected (Batch 2)

- **"Homepage `sizes=100vw` over-fetches at 50vw"**: rejected as stated — the
  work grid is genuinely full-bleed, so `100vw` is ~right; only the `--bleed`
  gutter over-fetches, which is what plan 008 actually fixes (smaller win).
- **"Sitemap missing `/world/*` subpages"**: false — they're in `ALL_PROJECTS`
  and already in the sitemap. Only `/contact` was missing (plan 010).
- **"Pre-compress case PNGs for user bandwidth"**: re-scoped (plan 009), not
  rejected — users get `next/image` WebP/AVIF, so source compression is a
  build/deploy win (P3), not a Core Web Vitals win.

---

# Batch 3 — Lint hygiene + execute deferred media work (2026-06-23, branch `dev-opt`, base `221f66a`=main)

Scoped via `/plan-eng-review` against the *current* `main` (Batch 1 + 2 are now
merged via PR #46). The user selected two themes: **(A) CI/correctness hygiene**
and **(B) media weight**. Batch B reuses the existing, still-valid plans 005 and
009 — both are now **unblocked** (`ffmpeg 8.0.1` is installed). DRY/token cleanup
(Batch C) and the reliability/UX backlog (Batch D) were considered and deferred
by the user this round.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 013  | Make `npm run lint` exit 0 (4 react-hooks errors + dead code) | P1 | S–M | LOW | — | DONE (commit `9f9b0fd`, branch `dev-opt`) — lint/typecheck/build/test green, 4 fixes browser-verified |
| 005  | Shrink + harden self-hosted videos | P1 | M | MED | see cache note | DONE (commit `5f56c41`, branch `dev-opt`) — 5 videos re-encoded + cache-busted (62.8M→8.8M), tags hardened, dup removed; browser-verified |
| 009  | Compress oversized source images | P3 | S | LOW | — | DONE (commit `4d742fa`, branch `dev-opt`) — 9 JPGs 34M→6.7M, dims preserved; PNGs skipped (no ImageMagick) |

Recommended order: **013 → 005 → 009**. 013 first so the lint gate is green
before the media changes land (each media plan lists "lint exits 0" criteria that
only become real after 013). 005 before 009 is arbitrary (independent), but do
009 last since it is the lowest-value (P3, build/deploy weight only).

## Tooling status (verified 2026-06-23 on `dev-opt`)

- `ffmpeg` **8.0.1** present → unblocks 005 (video re-encode) and 009 (JPG re-encode).
- `cwebp` present; `avifenc` and ImageMagick (`magick`/`convert`) **absent**.
  → 009 Step 3 (PNG lossless re-encode) **skips** without ImageMagick. The biggest
  rasters are JPGs (`atlans-3-1.jpg` 7.8 MB, `atlans-3-4.jpg` 7.0 MB,
  `orb-4-1.jpg` 4.2 MB), so JPG-only still captures most of 009's win. To also
  shrink the multi-MB PNGs, install ImageMagick first (`brew install imagemagick`).

## ⚠ Cache-immutability ordering risk (affects 005) — DECISION NEEDED

Plan 005's README note said "land 005 before 006." That ordering is now **moot**:
plan 006 is already merged, so the 1-year `public, max-age=31536000, immutable`
header on `/assets/*` (`next.config.ts:24`) is **live**. Plan 005 re-encodes
videos **in place** (same filename). Consequence: a returning visitor whose
browser already cached the old 16–19 MB file under the immutable policy **will not
refetch** the smaller version for up to a year. New visitors and cache-evicted
clients get the small files immediately.

**DECISION (2026-06-23): cache-bust.** Plan 005 has been updated to re-encode the
6 large videos to **content-hashed filenames** and update their `src` references
in `app/world/{id,money}/page.tsx`, then delete the originals. This guarantees
every visitor — including returning ones — fetches the smaller file immediately,
per the "rollout safety / no stale pinned asset" reliability rule. The original
"re-encode in place, no code changes" approach is superseded.

## Findings deferred this round (Batch C / D — not selected)

- **Batch C — DRY/tokens** (tech-debt, M): `FONT` const in 7 files; `#282328`
  hardcoded 42× / `#F0EBE5` 108× with no shared tokens; `SOCIALS` array duplicated
  in `contact/ContactForm.tsx` + `contact/page.tsx`; typography object drift
  (studio body 24px vs CaseStudy 22px); needless `"use client"` on `FooterBanner`,
  `Reveal`, `WorldCards`. Real, low-risk maintainability; no user impact.
- **Batch D — reliability/UX** (mixed): contact Server Action has no rate limiting
  (see `TODOS.md`, blocked on a KV/Redis store + observed abuse); dead social
  links `href="#"` (blocked on real URLs); `console.error`-only logging in
  `actions.ts` (structured-telemetry backlog).
